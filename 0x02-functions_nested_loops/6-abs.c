#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @i: argument passed must be an type int
 * Return: the value of the computation
 */
int _abs(int i)
{
	int ret;

	ret = _abs(i);
	_putchar(i);
	return (ret);
}
